<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.idic.login">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
            android:name=".LoginApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.idic.login.ui.activity.CheckInfoActivity" />
        <activity android:name="com.idic.login.ui.activity.LoginActivity" />
        <activity android:name="com.idic.login.ui.activity.NetErrorActivity" />
        <activity android:name="com.idic.login.ui.activity.SplashActivity"/>
    </application>

</manifest>